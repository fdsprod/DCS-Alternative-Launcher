<UserControl x:Class="DCS.Alternative.Launcher.Plugins.Game.Views.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:behaviors="clr-namespace:DCS.Alternative.Launcher.Behaviors"
             xmlns:cef="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
             xmlns:controls="clr-namespace:DCS.Alternative.Launcher.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:data="clr-namespace:DCS.Alternative.Launcher.Data"
             xmlns:fluentWpf="clr-namespace:SourceChord.FluentWPF;assembly=FluentWPF"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:wpf="clr-namespace:LoadingIndicators.WPF;assembly=LoadingIndicators.WPF"
             x:Name="RootLayout"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             HorizontalContentAlignment="Stretch"
             VerticalContentAlignment="Stretch"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="pack://application:,,,/LoadingIndicators.WPF;component/Styles/LoadingWave.xaml" />
                <ResourceDictionary
                    Source="pack://application:,,,/LoadingIndicators.WPF;component/Styles/LoadingThreeDots.xaml" />
                <ResourceDictionary
                    Source="pack://application:,,,/LoadingIndicators.WPF;component/Styles/LoadingFlipPlane.xaml" />
                <ResourceDictionary
                    Source="pack://application:,,,/LoadingIndicators.WPF;component/Styles/LoadingPulse.xaml" />
                <ResourceDictionary
                    Source="pack://application:,,,/LoadingIndicators.WPF;component/Styles/LoadingDoubleBounce.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="Foreground" Value="#FFFFFFFF" />
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid
            Visibility="{Binding IsLoading.Value, Converter={x:Static data:VisibilityConverter.Instance}, ConverterParameter=reverse}">
            <StackPanel Height="225"
                        Margin="30,50,0,0"
                        VerticalAlignment="Top"
                        Orientation="Horizontal">
                <controls:NewsArticleControl Width="270"
                                             Height="175"
                                             Margin="0,0,20,0"
                                             DataContext="{Binding LatestNewsArticle.Value}" />
                <controls:NewsArticleControl Width="270"
                                             Height="175"
                                             Margin="0,0,20,0"
                                             DataContext="{Binding PreviousNewsArticle.Value}" />

                <Border Width="375"
                        Height="200"
                        Margin="0,0,20,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Bottom"
                        Background="#FF141819"
                        BorderBrush="#FF2F2F2F"
                        BorderThickness="1">

                    <cef:ChromiumWebBrowser HorizontalAlignment="Stretch"
                                            VerticalAlignment="Stretch"
                                            Address="{Binding LatestEagleDynamicsYouTubeUrl.Value}"
                                            Background="Black" />
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="8" ShadowDepth="0" />
                    </Border.Effect>
                </Border>
            </StackPanel>
            <StackPanel Margin="30"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Bottom"
                        Orientation="Horizontal">
                <Button Margin="0,0,25,0"
                        Padding="20"
                        Background="#6F1CC600"
                        Command="{Binding LaunchDcsCommand}"
                        Foreground="White"
                        Style="{StaticResource ButtonRevealStyle}">
                    <TextBlock FontFamily="{StaticResource Orbitron}"
                               FontSize="18"
                               Text="LAUNCH DCS">
                        <TextBlock.Effect>
                            <DropShadowEffect BlurRadius="8" ShadowDepth="0" />
                        </TextBlock.Effect>
                    </TextBlock>
                    <Button.Effect>
                        <DropShadowEffect BlurRadius="8" ShadowDepth="0" />
                    </Button.Effect>
                </Button>
                <Button Margin="0,0,25,0"
                        Padding="15"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Bottom"
                        Background="#6F1C00C6"
                        Command="{Binding RepairDcsCommand}"
                        Foreground="White"
                        Style="{StaticResource ButtonRevealStyle}">
                    <TextBlock FontFamily="{StaticResource Orbitron}"
                               FontSize="16"
                               Text="REPAIR DCS">
                        <TextBlock.Effect>
                            <DropShadowEffect BlurRadius="8" ShadowDepth="0" />
                        </TextBlock.Effect>
                    </TextBlock>
                    <Button.Effect>
                        <DropShadowEffect BlurRadius="8" ShadowDepth="0" />
                    </Button.Effect>
                </Button>
            </StackPanel>
        </Grid>
        <wpf:LoadingIndicator IsActive="{Binding IsLoading.Value}" Style="{DynamicResource LoadingIndicatorArcsStyle}" />
    </Grid>
</UserControl>